---
import BaseLayout from '@/layouts/BaseLayout.astro';
import Header from '@/components/Header.astro';
import Footer from '@/components/Footer.astro';
---

<BaseLayout>
  
  <main id="homepage">
    <!-- Hero Section -->
    <section class="hero">
      <h1>Smart AI Product Strategy</h1>
      <p class="tagline">Data-driven clarity. Human-centered design. Measurable results.</p>
    </section>

    <!-- About Section -->
    <section id="about" class="about">
      <h2>Who We Are</h2>
      <p class="about-lede">
        Akradé is an AI-forward product strategy and human-centered design practice that helps
        organizations clarify vision, shape intelligent digital products, and deliver meaningful
        business outcomes. 
      </p>
      <p>
        We bring together AI consulting, product management expertise, 
        and user experience design to support teams through clear strategy, strong product leadership, 
        and modern design execution. By blending AI, systems thinking, and design-led product development, 
        Akradé helps companies accelerate innovation, reduce complexity, and build digital 
        solutions with confidence, clarity, and measurable impact.
      </p>
    </section>

    <!-- Services Section -->
    <section id="services" class="services">
      <h2>What We Offer</h2>
      <div class="services-grid">
        <div class="service-card">
          <h3>AI Product Strategy</h3>
          <p>Strategic roadmap development powered by AI insights and market intelligence.</p>
        </div>
        <div class="service-card">
          <h3>Product Management</h3>
          <p>End-to-end product leadership from strategy through delivery and iteration.</p>
        </div>
        <div class="service-card">
          <h3>Experience Design</h3>
          <p>User flow refinement and experience design that puts humans first.</p>
        </div>
        <div class="service-card">
          <h3>Product Definition</h3>
          <p>Clear requirements development and product scoping for confident execution.</p>
        </div>
        <div class="service-card">
          <h3>Discovery & Research</h3>
          <p>User interviews, competitive analysis, and market validation to inform product direction.</p>
        </div>
        <div class="service-card">
          <h3>Design Systems</h3>
          <p>Scalable component libraries and design standards for consistent product experiences.</p>
        </div>
      </div>
    </section>

    <!-- Contact Section -->
   <section id="contact" class="contact">
    <h2>Stay Connected</h2>
    <div class="contact-grid">
      <div class="contact-item">
        <p class="contact-label">Email</p>
        <a href="mailto:contact@akrade.com">hello@akrade.com</a>
      </div>
      <div class="contact-item">
        <p class="contact-label">Meet</p>
        <a href="#"
          id="contact-schedule-link"
          data-trigger="schedule-panel">
          Schedule a Meeting
        </a>
      </div>
      <div class="contact-item">
        <p class="contact-label">Call</p>
        <a href="tel:+14047819552">404-781-9552</a>
      </div>
      <div class="contact-item">
        <p class="contact-label">Find Us</p>
        <p>Atlanta, Georgia</p>
      </div>
    </div>
    </section>
  </main>

</BaseLayout>

<script>
  // Handle Schedule a Meeting link in contact section
  const contactScheduleLink = document.getElementById('contact-schedule-link');

  contactScheduleLink?.addEventListener('click', (e) => {
    e.preventDefault();
    // Call the global function exposed by ScheduleCallPanel
    if (typeof (window as any).openSchedulePanel === 'function') {
      (window as any).openSchedulePanel();
    }
  });
</script>

<style>


  /* Hero */
  .hero {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
  }

  .hero h1 {
    font-size: clamp(3rem, 10vw, 6rem);
    font-weight: 700;
    letter-spacing: -0.02em;
    line-height: 1.1;
    margin-bottom: 1rem;
  }

  .tagline {
    font-size: clamp(1.125rem, 3vw, 1.5rem);
    color: var(--color-accent-secondary);
  }

  /* About */
  .about {}

  h2 {
    font-size: 2rem;
    margin-bottom: 2rem;
    font-weight: 600;
  }

  .about p {
    font-size: 1.25rem;
    color: var(--color-accent-secondary);
    max-width: 800px;
    line-height: 1.8;
  }

  .about .about-lede {
    color: var(--color-white);
    font-style: italic;
    font-family: var(--font-serif);
    font-size: 2rem;
    line-height: 1.3;
    margin-bottom: 2rem;
  }


  /* Services */
  .services {}

  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
  }

  .service-card {
    padding: 2rem;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 8px;
    transition: border-color 0.2s;
    background-color: rgba(255, 255, 255, 0.02);;
  }

  .service-card:hover {
    border-color: var(--color-accent-secondary);
    background-color: rgba(255, 255, 255, 0.035);;
  }

  .service-card h3 {
    font-size: 1.25rem;
    margin-bottom: 0.75rem;
  }

  .service-card p {
    color: var(--color-accent-secondary);
  }

  /* Mobile: Reduce font sizes and simplify layout */
  @media (max-width: 480px) {
    h2 {
      font-size: 1.5rem;
      margin-bottom: 1.5rem;
    }

    .about p {
      font-size: 1rem;
      line-height: 1.6;
    }

    .about .about-lede {
      font-size: 1.25rem;
      line-height: 1.4;
      margin-bottom: 1.5rem;
    }

    .services-grid {
      display: block;
      gap: 0;
      margin-top: 2rem;
    }

    .service-card {
      padding: 0;
      border: none;
      border-radius: 0;
      background-color: transparent;
      margin-bottom: 2rem;
    }

    .service-card:hover {
      border-color: transparent;
      background-color: transparent;
    }

    .service-card:last-child {
      margin-bottom: 0;
    }

    .service-card h3 {
      font-size: 1rem;
      margin-bottom: 0.5rem;
    }

    .service-card p {
      font-size: 0.9rem;
    }

    .contact-item a,
    .contact-item p:not(.contact-label) {
      font-size: 1.125rem;
    }
  }

  /* Contact */
  .contact {}

  .contact-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1rem;
    margin-top: 3rem;
  }

  .contact-item {
    text-align: left;
  }
  .contact-item > p:nth-of-type(2) {
    margin: 0;
  }

  @media (max-width: 1000px) {
    .contact-grid {
      grid-template-columns: 1fr;
      gap: 0;
    }

    .contact-item:not(:last-child) {
      margin-bottom: 2rem;
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
      padding-bottom: 2rem;
    }
  }

  .contact-label {
    text-transform: uppercase;
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    color: var(--color-muted);
    margin-bottom: 0.5rem;
  }

  .contact-item a,
  .contact-item p:not(.contact-label) {
    font-size: 1.5rem;
    color: var(--color-teal);
    text-decoration: none;
  }
  .contact-item p:not(.contact-label) {
    color: var(--color-text);
  }
  
  .contact-item a:hover {
    color: var(--color-accent-secondary);
  }
</style>
